.list-group
  - user.subscriptions.each do |subscription|
    %a{ class: 'list-group-item',
        href:  feed_path(subscription.feed) }
      = "#{subscription.feed.title}"
      %span.badge= current_user.unread_count(subscription.feed)
