%h1= "#{current_user.name}'s subscriptions"

- @subscriptions.each do |subscription|
  %h2= link_to subscription.feed.title, subscription.feed.url
  %span= link_to 'delete', user_subscription_path(current_user, subscription), method: :delete
